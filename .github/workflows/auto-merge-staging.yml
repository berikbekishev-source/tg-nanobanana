name: Auto merge feature → staging PRs (disabled)

on:
  workflow_run:
    workflows: ["CI and Smoke"]
    types: [completed]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Автослияние отключено
        run: |
          echo "Merge в staging выполняется вручную через gh CLI после бронирования стенда (см. STAGING_STATUS.md)."
